{% extends 'base.html.twig' %}

{% block title %}Détail{% endblock %}

{% block body %}

<div class="card sm-3 md-4 lg-8">
      <div class="row g-0">
   
    
         {% for d in detail.detail %}
        
                        <div class="col-sm-7 col-md-6 col-lg-5">
                           <div>                           
                            <img class="imgd" src='/images/illu{{ d.id }}.jpg'/>
                             </div>
                            </div>
                            <div class="col-sm-6 col-md-4 col-lg-6">
                          
                                <h4 class="text-center" >                              
                               {{ d.libelle }}
                                </h4> 
                              
                                 <div class="row">
                                 <div class="col-md-3 offset-md-3"> {{ d.prix }} € </div>
                                
                               <div class="col-md-3 offset-md-3"> <a href="/add/{{ d.id }}" class="btn btn-success">Ajouter au panier</a></div>
                                </div>
                            </a>
                                   <div class="card sm-3 md-5 lg-8">
                        <div class="card-body">
                        {{ d.description }}
                    </div>
                   </div>
                        </div>
                 
          
                       
                   
                {% endfor %}
        </div>
    </div>
      

{% endblock %}